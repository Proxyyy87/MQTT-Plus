{
  "name": "iobroker.mqtt-plus",
  "version": "1.6.1",
  "description": "Bridge between ioBroker states and MQTT with JSON export and Remote Sync",
  "author": "proxy <zumloeschen@ich.ms>",
  "license": "MIT",
  "homepage": "https://github.com/Proxyyy87/MQTT-Plus#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Proxyyy87/MQTT-Plus.git"
  },
  "bugs": {
    "url": "https://github.com/Proxyyy87/MQTT-Plus/issues"
  },
  "main": "build/main.js",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "admin/",
    "build/",
    "io-package.json"
  ],
  "keywords": [
    "iobroker",
    "mqtt",
    "bridge",
    "export",
    "sync"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "prepare": "tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@iobroker/adapter-core": "^3.2.2",
    "axios": "^1.20.0"
  },
  "devDependencies": {
    "@iobroker/testing": "^4.1.3",
    "@iobroker/types": "^6.0.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  },
  "type": "commonjs"
}
